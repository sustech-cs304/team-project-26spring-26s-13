# ── Backend ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.0.0     # BaseSettings for config.py
pydantic-ai>=0.0.13          # Agentic loop orchestration

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0              # async PostgreSQL driver
alembic>=1.13.0              # DB migrations
chromadb>=0.5.0              # vector DB
sentence-transformers       # multilingual embeddings (chromadb)

# File handling
python-multipart>=0.0.9     # multipart upload in FastAPI
pymupdf>=1.24.0             # PDF parsing (PyMuPDF)
python-docx>=1.1.2          # DOCX parsing
python-pptx>=0.6.23         # PPT parsing
numpy>=1.26.0,<2.0
pillow>=10.0.0

# Web scraping
httpx>=0.27.0               # async HTTP (scraping + internal)
aiohttp>=3.9.0              # async HTTP (unit-testable login flow)
beautifulsoup4>=4.12.0
selenium>=4.20.0

# Testing HTTP mocks
responses>=0.25.0
aioresponses>=0.7.0

# Security
cryptography>=42.0.0        # Fernet encryption for CAS/API keys
passlib[bcrypt]>=1.7.4      # bcrypt password hashing
bcrypt==4.0.1               # passlib 1.7.4 expects bcrypt's legacy __about__ metadata
python-jose[cryptography]>=3.3.0  # JWT tokens

# ── Observability (pydantic-ai / logfire LLM trace) ──────
opentelemetry-api>=1.39.0
opentelemetry-sdk>=1.39.0
opentelemetry-proto>=1.39.0
opentelemetry-exporter-otlp-proto-common>=1.39.0
opentelemetry-exporter-otlp-proto-http>=1.39.0
opentelemetry-exporter-otlp-proto-grpc>=1.39.0

# ── Frontend ─────────────────────────────────────────────
PyQt6>=6.7.0
requests>=2.31.0            # sync HTTP calls from PyQt6 worker threads

# ── OCR (installed separately due to protobuf conflict) ──
# paddlepaddle==2.6.2 requires protobuf<=3.20.2 on Windows, but
# opentelemetry-proto>=1.39 requires protobuf>=5.0.  These constraints
# are mutually exclusive, so pip cannot resolve them together.
# Install paddle packages with --no-deps after the main requirements:
#   pip install --no-deps paddleocr==2.7.3 paddlepaddle==2.6.2
# The runtime conflict is handled via PROTOCOL_BUFFERS_PYTHON_IMPLEMENTATION=python
# (set in backend/main.py and backend/seed_public_knowledge.py).
